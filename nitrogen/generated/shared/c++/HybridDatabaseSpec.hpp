///
/// HybridDatabaseSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QueryResult` to properly resolve imports.
namespace margelo::nitro::duckdb { struct QueryResult; }
// Forward declaration of `HybridPreparedStatementSpec` to properly resolve imports.
namespace margelo::nitro::duckdb { class HybridPreparedStatementSpec; }

#include <string>
#include "QueryResult.hpp"
#include <NitroModules/Promise.hpp>
#include <memory>
#include "HybridPreparedStatementSpec.hpp"

namespace margelo::nitro::duckdb {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Database`
   * Inherit this class to create instances of `HybridDatabaseSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridDatabase: public HybridDatabaseSpec {
   * public:
   *   HybridDatabase(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridDatabaseSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridDatabaseSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridDatabaseSpec() override = default;

    public:
      // Properties
      virtual std::string getPath() = 0;
      virtual bool getIsOpen() = 0;
      virtual bool getIsReadOnly() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<QueryResult>> execute(const std::string& sql) = 0;
      virtual QueryResult executeSync(const std::string& sql) = 0;
      virtual std::shared_ptr<HybridPreparedStatementSpec> prepare(const std::string& sql) = 0;
      virtual std::shared_ptr<Promise<void>> beginTransaction() = 0;
      virtual std::shared_ptr<Promise<void>> commit() = 0;
      virtual std::shared_ptr<Promise<void>> rollback() = 0;
      virtual std::shared_ptr<Promise<void>> loadExtension(const std::string& extensionName) = 0;
      virtual std::shared_ptr<Promise<void>> installExtension(const std::string& extensionName) = 0;
      virtual void close() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Database";
  };

} // namespace margelo::nitro::duckdb
